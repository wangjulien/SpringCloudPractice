#centreon-host-FEIGN
spring:
  main:
    banner-mode: "off"
  cloud:
    config:
      name: centreon-host-feign-client
      discovery:
        service-id: config-server
        enabled: true