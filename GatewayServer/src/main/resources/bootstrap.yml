spring:
  main:
    banner-mode: "off"
  cloud:
    config:
      name: gateway
      uri: http://localhost:8888
      username: root
      password: Telino_2018
      fail-fast: true
#      discovery:
#        service-id: config-server
#        enabled: true