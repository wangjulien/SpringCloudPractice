spring:
  main:
    banner-mode: "off"
  profiles:
    active: development
  cloud:
    config:
      name: config-client
      uri: http://localhost:8888
#      username: root
#      password: Telino_2018
      fail-fast: true