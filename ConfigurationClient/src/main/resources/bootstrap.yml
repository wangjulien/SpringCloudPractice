spring:
  main:
    banner-mode: "off"
  application: 
    name: config-client
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:8888
      username: root
      password: Telino_2018
      fail-fast: true